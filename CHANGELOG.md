# Changelog

All notable changes are documented here.

## 0.1.0 — Unreleased

- Initial typed TypeScript and Node.js evidence-capture SDK.
- Append-only recorder, bounded lifecycle helpers, strict Core v1.0 JSON validation, canonical
  fingerprints, deterministic OTLP JSON bridge, sealed fixtures, and offline contract tests.
